export enum TechStackEnum {
  FRONTEND = "frontend",
  BACKEND = "backend",
  MACHINE = "machine",
  CLOUDDEV = "cloudDev",
  TESTING = "testing",
  OPERATING = "operating",
  TOOLS = "tools",
}

export enum ThemeTypesEnum {
  DARK = "dark",
  LIGHT = "light",
}
